import { Component } from '@angular/core';

@Component({
  selector: 'dpo-login',
  standalone: true,
  template: '<section class="page" data-cy="login-page">Login placeholder</section>',
  styles: [
    '.page { padding: var(--space-xl); color: var(--color-text-secondary); font-size: var(--font-size-md); }'
  ]
})
export class LoginComponent {}
