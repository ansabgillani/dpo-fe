# FileRowComponent

Single file row with status tag and action icons.

## Inputs
- `file`: file entry data.
- `disabled`: disables row actions.

## Outputs
- `uploadSelected({ fileId, file })`
- `downloadRequested(fileId)`
- `deleteRequested(fileId)`
