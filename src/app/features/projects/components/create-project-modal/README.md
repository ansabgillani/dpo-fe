# Create Project Modal

Modal form for creating a new project from the Projects landing page.

## Responsibilities
- Capture required fields: title, department, business line, type, start date, end date.
- Capture optional fields: image and PSP elements.
- Validate required fields before emitting create.
- Emit cancel/create actions to parent orchestration component.
